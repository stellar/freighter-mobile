import { BaseLayout } from "components/layout/BaseLayout";
import { Text } from "components/sds/Typography";
import React from "react";

/**
 * Placeholder for the Earn token picker. Replaced wholesale in Task 9; it
 * exists so the navigator compiles and the Home entry point can be verified
 * in isolation.
 */
export const EarnTokenPickerScreen = () => (
  <BaseLayout>
    <Text md>Earn</Text>
  </BaseLayout>
);

export default EarnTokenPickerScreen;
